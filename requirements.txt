# ---------------------------------------------------------------------------
# Hard Constraint 1 of the brief: NO agentic frameworks.
# LangChain, LangGraph, CrewAI, AutoGen and equivalents are deliberately
# absent. The reasoning layer in app/reasoning/ is hand-written and talks to
# the LLM through Groq's plain SDK only.
# ---------------------------------------------------------------------------

# --- Detection (Hard Constraint 2: RT-DETR, own training code) ---
ultralytics==8.3.40
torch>=2.2
torchvision>=0.17

# --- Dataset preparation ---
datasets>=2.19,<4.0.0
pillow>=10.0
numpy>=1.26
tqdm>=4.66

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# --- Reasoning layer (plain SDK, no framework) ---
# Groq's own SDK. It is a thin HTTP client over their chat-completions
# endpoint - it does not chain, plan, or orchestrate anything. All of the
# routing and reasoning logic lives in app/reasoning/ and is mine.
groq>=0.11

# --- Evaluation / reporting ---
matplotlib>=3.8
pandas>=2.2

# --- Dev ---
pytest>=8.0
httpx>=0.27
